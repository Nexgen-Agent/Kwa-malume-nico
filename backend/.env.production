​Database Configuration
​DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/malume_prod
​Security & Authentication
​SECRET_KEY=your-production-super-secret-key-from-secrets-token_urlsafe-32
ENVIRONMENT=production
DEBUG=false
​CORS Origins (frontend URLs)
​CORS_ORIGINS=https://yourdomain.com,[suspicious link removed]
​Database Pooling (for PostgreSQL in production)
​DB_POOL_SIZE=20
DB_MAX_OVERFLOW=10
DB_POOL_RECYCLE=3600
​SSL (for production)
​DB_SSL=true
​Email Service (optional - for notifications)
​SMTP_SERVER=
SMTP_PORT=
SMTP_USERNAME=
SMTP_PASSWORD=
NOTIFICATION_EMAIL=